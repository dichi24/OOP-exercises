string sætning = "Jeg vil gerne bestille en lagkage med ekstra flødeskum"; // 46

int Bogstaver()
{
	int letterCount = CountLetters(sætning);
	
	Console.WriteLine("Der er" + "letterCount" + "bogstaver");
	return letterCount;
}

